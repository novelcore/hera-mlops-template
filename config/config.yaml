# The config tree — root defaults list (Hydra).
# Each group choice here is a DEFAULT; every group renders as one dropdown on
# the submit form (options = the files in the group dir). Add your own config
# groups here as your pipeline grows.
defaults:
  - _self_
  - experiment
  - platform: live # platform-owned group (searchpath); NOT a form surface
